PORT=3000

# Email (SMTP) — use SendGrid SMTP, Mailgun SMTP, or Gmail (app password)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey          # for SendGrid, use "apikey"
SMTP_PASS=YOUR_SENDGRID_API_KEY_OR_SMTP_PASS
FROM_EMAIL=orders@yourdomain.com

# Owner contact
OWNER_EMAIL=asam@gmail.com
OWNER_PHONE=+923136710919

# Twilio (for SMS)
TWILIO_SID=your_twilio_account_sid
TWILIO_TOKEN=your_twilio_auth_token
TWILIO_FROM=+1yourTwilioNumber

# set SERVE_STATIC=true if you serve index.html from ./public
SERVE_STATIC=true